import math
radius = float(input("Enter Radius: ")) # Input the Radius
circumference = float(2*math.pi*radius) # Calculating Circumference
print(f"Circumference : {round(circumference , 2)}cm") # Round Function for last 2 digit